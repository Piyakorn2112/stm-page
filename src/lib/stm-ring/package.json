{
  "name": "@stm/ring",
  "version": "0.0.0",
  "private": true,
  "description": "STM generative-ring system — deterministic, animated, pure-SVG ring identities.",
  "//": "Future package. Today it is consumed in-repo via the @stm-ring tsconfig path alias (-> ./src). When extracted, set main/types/exports to the built ./dist and publish; consumers import from the same barrel so nothing else changes.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "peerDependencies": {
    "react": ">=19",
    "react-dom": ">=19"
  },
  "sideEffects": false
}
