# Keep the build context small + reproducible (the image installs from the
# lockfile, so host node_modules/.next must never leak in).
node_modules
.next
npm-debug.log*
.git
.gitignore
.github
Dockerfile
.dockerignore
deploy
README.md
*.md
.env*
.vscode
.idea
